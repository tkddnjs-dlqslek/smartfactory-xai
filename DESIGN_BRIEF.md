# SmartFactory XAI — Design Brief

> Claude design 작업 컨텍스트
> 사출성형 24센서 이상탐지·진단·처방 통합 플랫폼 UI/UX 디자인

---

## 1. 제품 한 문장

**"라인이 멈추기 전에 AI가 먼저 알린다"** — 사출성형기 24개 센서 데이터를 실시간 분석해 이상을 탐지하고, 어떤 센서가 원인인지 진단하며, 처방까지 자동 제공하는 산업용 통제 대시보드.

---

## 2. 디자인 톤 & 철학

### 톤 (한 줄)
> **"NASA mission control × Bloomberg terminal × Tesla autopilot"**

### 5가지 디자인 원칙
1. **정밀 (Precision)** — 모든 수치는 소수점까지, 단위 명시, monospace 폰트로 정렬
2. **신뢰 (Trust)** — 통계 신뢰구간·검증 방법·실측 vs 추정치 명시
3. **통제 (Control)** — 산업 현장의 통제된 긴장감, 소비자 친화 X
4. **위계 (Hierarchy)** — 데이터 밀도 높지만 위험 신호는 즉시 인지 가능
5. **무채색 기본 (Neutral Default)** — 정상 상태는 회색, 색상은 위험·강조 전용

### 절대 피할 것
- 그라데이션 배경 (단색만)
- 둥근 모서리 8px 초과 (대부분 0~6px)
- 일러스트레이션, 캐릭터, 이모지 과사용
- 채도 높은 색 (네온 파랑·보라·핑크·노랑·녹색 X)
- 그림자·블러 (Material Design 톤 X)
- 애니메이션 과도 (펄스·페이드 정도만)
- "재미있는" UI 텍스트 (모든 카피는 사실 기반)

---

## 3. 타깃 사용자 (3 페르소나)

| 페르소나 | 누구 | 사용 빈도 | 핵심 니즈 |
|---|---|---|---|
| **작업자 (Operator)** | 사출성형 3교대 현장 작업자, 5년차, 시급 12,000원 | **상시 (실시간)** | 이상 발생 → 즉시 인지 → 처방 카드 따라 조치 |
| **반장 (Foreman)** | 라인 관리자 | **매 교대 (8시간)** | 이력 점검 → CSV 다운로드 → 다음 교대 인수인계 |
| **부서장 (Manager)** | 생산기술 부서장 | **일·월** | 트렌드 분석 → 정비 권고 → 월 KPI 보고 |

→ **주 타깃은 작업자**. 그가 한 화면에서 "이상이다 / 왜 이상이다 / 뭘 해야 한다" 3초 안에 파악할 수 있어야 함.

---

## 4. 디자인 토큰

### 컬러 시스템 (다크 인더스트리얼)
```css
/* 배경 계층 (3-tier) */
--bg:        #080808;   /* 최외곽, deepest */
--card:      #111111;   /* 카드 배경 */
--card2:     #1A1A1A;   /* 보조 카드 / 호버 */

/* 텍스트 (3-tier) */
--text:      #EFEFEF;   /* 본문, primary */
--dim:       #888888;   /* 부가 정보, secondary */
--muted:     #4A4A4A;   /* 비활성, tertiary */

/* 시그널 (절제 — 2색만) */
--red:       #D42121;   /* ⚠ 위험·알람 전용 */
--red-bg:    rgba(212,33,33,0.12);
--red-bd:    rgba(212,33,33,0.35);
--accent:    #00D4FF;   /* AI 점수·정보 강조 */
--accent-bg: rgba(0,212,255,0.08);

/* 보조 (최소 사용) */
--ok:        #4CAF50;   /* 정상 표기 */
--warn:      #FFA500;   /* 경고 (위험 아래 단계) */
--critical:  #8B0000;   /* 긴급 (위험 위 단계) */

/* 구조 */
--border:    rgba(255,255,255,0.07);  /* 카드 보더 (반투명) */
--grid:      rgba(255,255,255,0.04);  /* 차트 그리드 (매우 흐림) */
--line:      rgba(255,255,255,0.06);  /* 구분선 */
```

### 타이포그래피
```
한글:     Pretendard (weights 400, 500, 600, 700)
영문:     Inter (weights 400, 500, 600, 700)
수치/모노: JetBrains Mono (weights 400, 500)

/* 사이즈 위계 */
Hero:        1.8rem  weight 700  letter-spacing -0.02em
H1 (탭명):   1.05rem weight 700  letter-spacing -0.01em
H2 (섹션):   0.67rem weight 600  uppercase letter-spacing 0.10em
KPI 값:      1.4rem  weight 700  Mono
KPI 라벨:    0.65rem weight 600  uppercase letter-spacing 0.09em
본문:        0.82rem weight 500
캡션:        0.72rem weight 400  color: dim
```

### 간격 (8px baseline)
- 컴포넌트 padding: 8 / 12 / 16 / 20 / 24
- 카드 사이 gap: 8 / 12 / 16
- 페이지 padding: 0.8rem (top) / 2rem (bottom)

### 보더 / 라운드
- `border-radius`: 3px (배지) / 4px (작은 박스) / 6px (카드) — **최대 6px**
- 보더 두께: 1px (기본) / 2px (강조) / 3px (좌측 시그널 라인)
- 그림자: **없음** (산업미)

---

## 5. 화면 정보 아키텍처 (5탭 + 사이드바)

```
┌─ Sidebar (280px) ─────────────────────────────────────┐
│  ⚙ 설정                                                │
│   • 설비 선택 드롭다운                                   │
│   • 데모 시나리오 (정상 / 경고 / 위험 / 긴급)             │
│   • 🔴 LIVE 디지털 트윈 토글                            │
│   • LIVE 재생 컨트롤 (▶ / ⏸ / ⏮)                       │
├─ Main (flex) ─────────────────────────────────────────┤
│  탭 네비게이션 (5)                                       │
│  ┌─ 1. 실시간 진단 ─────────────────────────────────┐  │
│  │  운영 모드 선택 (균형 / 탐지우선 / 정밀)           │  │
│  │  ┌─ 좌측: 24 슬라이더 ─┬─ 우측: 판정 결과 ────┐  │  │
│  │  │  • 시간 (5)         │  ┌ AI 종합 배너 ────┐ │  │  │
│  │  │  • 위치 (3)         │  │ ✓ 정상 / 빨강 위험 │ │  │  │
│  │  │  • 속도/RPM (3)     │  └──────────────────┘ │  │  │
│  │  │  • 압력 (5)         │  ┌ 다중 AI 합의 ──────┐│  │  │
│  │  │  • 온도 (8)         │  │ AE IF OCSVM LOF  │ │  │  │
│  │  └─────────────────────┤  │ ▓▓░░ 4 모델 동의 │ │  │  │
│  │                        │  └──────────────────┘ │  │  │
│  │                        │  ┌ 반원 게이지 ────────┐│  │  │
│  │                        │  │ 복원 오차 vs 임계값 │ │  │  │
│  │                        │  └──────────────────┘ │  │  │
│  │                        │  KPI: 복원 오차 / 임계값│  │  │
│  │                        └──────────────────────┘  │  │
│  │  ─ 대응 권고 (이상 시만) ─                        │  │
│  │  #1 [센서명] (Xσ) [정비 필요] → 처방              │  │
│  │  #2 ...                                          │  │
│  │  #3 ...                                          │  │
│  │  ─ σ 이탈 차트 ─                                 │  │
│  │  ─ What-if 시뮬레이터 ─                           │  │
│  │  ─ AI 자연어 진단 보고서 ─                        │  │
│  │  ─ 이상 감지 이력 (Active Learning) ─             │  │
│  └─────────────────────────────────────────────────┘  │
│  ┌─ 2. 불량 원인 분석 (SHAP) ──────────────────────┐  │
│  │  좌측: 센서별 평균 SHAP 막대 차트 (Top-5 빨강)    │  │
│  │  우측: 상위 5 이상 원인 센서 카드                  │  │
│  │  하단: SHAP Waterfall · 인과 그래프 · PCA 클러스터│  │
│  └─────────────────────────────────────────────────┘  │
│  ┌─ 3. 전체 이력 일괄 분석 ─────────────────────────┐  │
│  │  복원 오차 시계열 + 이상 오차 상위 20건            │  │
│  └─────────────────────────────────────────────────┘  │
│  ┌─ 4. 생산 이력 ──────────────────────────────────┐  │
│  │  스코어카드 7개 (총 샷 / 이상 / 건강도 / 정비 권고) │  │
│  │  복원 오차 시계열 (1,379샷 전체)                  │  │
│  │  100샷 구간별 이상률 막대 차트                     │  │
│  │  Top 10 이상 집중 구간 표                          │  │
│  └─────────────────────────────────────────────────┘  │
│  ┌─ 5. 모델 신뢰도 확인 ────────────────────────────┐  │
│  │  5 KPI 스코어카드 (ROC/PR/F1/Recall/Precision)   │  │
│  │  오차 분포 · ROC · PR · Confusion Matrix         │  │
│  │  AI 아키텍처 다이어그램 · 검증 데이터셋 구성       │  │
│  │  학습 곡선 · 가설 검증 · 임계값 민감도            │  │
│  │  비용 의사결정 프레임 · 합의 알고리즘 비교 표      │  │
│  └─────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

---

## 6. 핵심 컴포넌트 패턴

### 6.1 KPI 스코어카드
```
┌──────────────────────┐
│ ROC-AUC              │  ← 라벨 (uppercase, dim)
│ 0.9254               │  ← 값 (큰 폰트, mono)
│ ↑ 95%CI [0.879, 0.966]│  ← 부가 (작은 폰트, accent or dim)
└──────────────────────┘
배경: --card, 보더: 1px --border, 좌측 3px 강조 라인
```

### 6.2 다중 AI 합의 미터
```
┌────────────────────────────────────────────────┐
│ 다중 AI 합의  [AE][IF][OCSVM][LOF]  3/4 · 강한 의심│
│ 기준: 다수결 (2/4↑)                              │
│ AUC-가중 Soft Score ────────────────── 0.847    │
│ ████████████████████░░░░░░                      │
└────────────────────────────────────────────────┘
각 모델 배지: 회색 (정상) / 빨강 (이상 동의)
```

### 6.3 반원 게이지 (복원 오차)
```
       ╱─────────╲
      ╱  0.4      ╲
   0.2            0.6      ← 빨간 세로선 = 임계값
     │            │
    ───────────────
배경: --card2, 정상 영역 흐림 회색, 위험 영역 흐림 빨강
바늘: 정상=#909090, 이상=빨강
```

### 6.4 처방 카드 (Top-3 이상 시만 표시)
```
┌─ #1 ──────────────────────────────────────────┐
│ [Filling_Time] (+4.2σ)  [정비 필요]            │  ← 좌측 빨간 3px 라인
│ 게이트 막힘 의심 · 수지 점도 확인 · 노즐 청소    │
└────────────────────────────────────────────────┘
배경: --card2, 좌측 보더 3px --red
```

### 6.5 심각도 배너 (4단계)
```
정상:  ✓ 정상 (NORMAL)        →  배경 transparent, 보더 회색
경고:  🟠 경고 (WARNING)       →  배경 #FFA500/12%, 보더 #FFA500
위험:  🔴 위험 (DANGER)        →  배경 --red-bg, 보더 --red
긴급:  🚨 긴급 (CRITICAL)      →  배경 #8B0000/15%, 보더 #8B0000
```

### 6.6 데이터 표 (Confusion Matrix)
```
              예측: 정상   예측: 불량
실제: 정상    1,334        6
실제: 불량       13       26
배경 셀 색상: 회색 그라데이션 (정확) / 빨강 그라데이션 (오류)
숫자: mono 폰트 22px
```

### 6.7 차트 스타일
- **Plotly dark 톤 강제**
- 축 라벨: 9px, color --dim
- 그리드: --grid 색, 흐리게
- 마커: 정상=회색 작은 점, 불량=빨강 다이아몬드
- 임계값 라인: --red 점선 1.5px
- 막대 차트: 강조=--red, 보조=--accent, 기타=#3A3A3A

---

## 7. 인터랙션 패턴

### 상태 전환
- **정상 → 이상**: 화면이 무채색에서 빨간 알람으로 전환. 처방 카드·What-if·자연어 보고서가 페이드 인 (0.3s)
- **운영 모드 전환**: 한 클릭으로 임계값 ×0.75 / ×1.0 / ×1.35 자동 조정 + 게이지 마커 이동
- **데모 시나리오 1클릭**: 24개 슬라이더가 KAMP 실측 불량 패턴으로 자동 세팅 (0.5s 슬라이드 이동 애니메이션)
- **LIVE 모드 ON**: 검증셋 1,379건이 10초 간격으로 자동 재생, 사이드바에 펄스 애니메이션

### 마이크로 인터랙션
- 게이지 바늘: 값 변경 시 0.4s ease-out
- KPI 값: 숫자 카운트업 (0.3s)
- 처방 카드: 등장 시 우→좌 슬라이드 (0.2s × 3개 순차)
- 알람 배너: 빨간 보더 1.5s 펄스 (1회만)

### 키보드 단축키 (옵션)
- `1~5`: 탭 전환
- `Space`: LIVE 재생/일시정지
- `R`: 시나리오 리셋 (정상 운영)

---

## 8. 디자인 레퍼런스

### 직접 참조
1. **Bloomberg Terminal** — 데이터 밀도, 무채색 + 빨강·시안 위계
2. **Linear** (linear.app) — 타이포·간격·다크 모드 톤
3. **Tesla Autopilot UI** — 산업 다크 + 빨강 알람
4. **Stripe Dashboard** — 차트 가독성, KPI 카드 패턴
5. **NASA Mission Control 화면** — 통제된 긴장감
6. **Bloomberg Terminal** (다시 — 정말 중요)

### 톤 영감
- Cursor IDE (코드 에디터 다크 + 강조 색)
- Vercel Analytics 대시보드 (미니멀 + 차트)
- Aritzia (NO) — 패션 톤은 절대 피함

---

## 9. 필요한 산출물 (우선순위)

### Phase 1: 본선 (~5/22) — 현재 Streamlit 유지
- 디자인 작업 없음, 현재 적용된 디자인 토큰 유지

### Phase 2: 본선 후 (~7월) — Next.js + Vercel 마이그레이션
1. **로고·브랜드 마크** ⭐⭐⭐
   - "4-AI 합의"를 상징하는 미니멀 마크 (4개 점이 합쳐지는 형태? / 톱니바퀴? / 게이지?)
   - 다크 배경에 잘 보이는 단색 + 강조색 1개
   - 16x16 favicon ~ 256x256 PNG 모두 사용 가능

2. **랜딩 페이지** (smartfactory-xai.vercel.app) ⭐⭐⭐
   - Hero: 제품 한 문장 + 시연 영상/이미지
   - 4-AI 합의 흐름도 (다이어그램)
   - 성능 KPI 5개 (ROC-AUC, Recall, Precision, F1, SHAP latency)
   - 시연 GIF (5분 데모 코스)
   - GitHub / Demo 링크
   - 팀 소개 (한 줄)

3. **5탭 대시보드 컴포넌트 라이브러리** ⭐⭐
   - 위 컴포넌트 패턴 (6.1~6.7)을 React/Tailwind 컴포넌트로
   - Storybook 또는 mockup HTML

4. **모바일·태블릿 뷰** ⭐⭐
   - 반장이 현장 태블릿에서 빠르게 이상 알람 확인하는 화면
   - 사이드바 → 햄버거 메뉴
   - 5탭 → 스와이프 또는 드롭다운
   - 24센서 슬라이더 → 핵심 5개만 노출, 나머지 expander

5. **발표 슬라이드 보강 이미지** ⭐
   - 아키텍처 다이어그램 (24센서 → AE+IF+OCSVM+LOF → SHAP → 처방)
   - 4-AI 합의 메커니즘 시각화
   - 본선 발표용

---

## 10. 핵심 메시지 (디자인에 녹일 카피)

### Hero 카피 (랜딩 페이지)
> **라인이 멈추기 전에 AI가 먼저 알린다**
> 사출성형 24센서, 4-AI 합의, KernelSHAP 설명 — 한 화면에서 탐지·진단·처방.

### 차별화 4가지 (서브 헤로)
- ① **공개 KAMP 데이터** — 재현 가능한 학술적 진정성
- ② **비지도 + KernelSHAP** — 라벨 없이도 설명 가능
- ③ **Cross-Machine + Bootstrap CI** — 통계 검증 강화
- ④ **24개 처방 통합** — 탐지·원인·조치를 단일 시스템에

### 발표 시 한 줄 임팩트
> "기존 솔루션이 '이상하다'에서 멈출 때,
> 우리는 '왜 · 얼마나 바꿔야 · 어떻게 보고할지'까지 답합니다."

---

## 11. 출력 형식 요청

- **HTML/CSS 또는 React/Tailwind 컴포넌트**로 산출
- 다크 모드 기본, 반응형 (mobile-first)
- 디자인 토큰 (위 색상·타이포·간격)을 CSS 변수 또는 Tailwind config로 적용
- 모든 컴포넌트는 **Pretendard + Inter + JetBrains Mono** 가정
- 상호작용 시각화는 Framer Motion 톤 (절제된 모션)

---

## 12. 참고 파일

- 현재 적용된 CSS: `assets/dashboard.css`
- 메인 코드: `app.py`
- 본선 PPT (시각 자료): `output/스마트공장XAI_예선기획서_최종.pdf`
- GitHub: https://github.com/tkddnjs-dlqslek/smartfactory-xai
