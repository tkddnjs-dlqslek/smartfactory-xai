# SmartFactory XAI 대시보드 사용 인수인계서 (신입사원용)

> 사출성형 라인에 새로 배치된 작업자/관리자가 이 대시보드를 **처음부터 끝까지 혼자 쓸 수 있도록** 만든 안내서입니다. IT 지식이 없어도 됩니다. 어려운 용어는 전부 풀어서 설명합니다.

---

## 0. 한 줄 요약

**24개 센서로 사출성형기를 감시하다가, 평소와 다르면 AI가 즉시 알려주고 "왜 그런지(원인)"와 "어떻게 고칠지(처방)"까지 알려주는 화면입니다.**

품질·설비·안전·생산 4개 영역을 한 화면에서 봅니다. 사람이 계속 안 봐도 기계 상태를 대신 봐주는 **AI 비서**입니다.

---

## 1. 먼저 알아야 할 용어

| 용어 | 쉬운 설명 |
|---|---|
| **센서** | 기계의 온도·압력·속도 등을 재는 장치. 우리는 24개를 봅니다. |
| **σ (시그마)** | "평소와 얼마나 다른가". 0이면 정상, 숫자가 클수록 비정상. +면 평소보다 높음, −면 낮음. |
| **복원 오차 (recon error)** | AI가 "정상이면 이래야 한다"고 예상한 값과 실제 값의 차이. 클수록 비정상. (오토인코더 1개의 값) |
| **임계값 τ (타우) = 0.320** | 복원오차가 이 선을 넘는지 보는 경계. (단독 판정이 아니라 4-AI 투표의 한 표) |
| **4-AI 합의** | 성격이 다른 AI 4개(AE·IF·OCSVM·LOF)가 **투표**해서, **3개 이상**이 동의하면 "이상"으로 판정. |
| **경고/위험/긴급** | 모두 "이상"입니다. 4-AI가 이상이라고 합의한 뒤, AE 복원오차 크기로 등급을 나눕니다. |
| **SHAP** | "어느 센서가 이상의 **원인**인지" 콕 집어주는 기술. 양수(+)일수록 이상을 일으킨 범인. |
| **What-if** | "이 센서를 정상으로 되돌리면 결과가 어떻게 바뀔까" 미리보기. |
| **OEE** | 설비 종합 효율 = 가동률 × 성능 × 양품률. |
| **준지도 학습** | 불량 사례가 너무 적어서(전체의 약 1%) **정상 데이터만** 학습. "정상의 모습"을 외우고 거기서 벗어나면 이상으로 판단. |
| **실측 / 가정** | 화면 태그. **실측**=진짜 측정·계산값, **가정**=데이터가 없어 가정한 값(참고용). |

> 💡 화면에서 **빨강**=이상/위험, **하늘색(시안)**=정상/양호, **회색**=보통.

---

## 2. 접속 방법

- **배포본:** 관리자에게 받은 Vercel 주소를 브라우저에 입력
- **사내 로컬:** 백엔드(8100)·프론트(3000) 서버가 켜져 있어야 합니다
- 화면이 멈춰 보이면 **Ctrl + Shift + R**(강력 새로고침)
- 화면 상단 부제에 **"⚠ 백엔드 미연결"**이 보이면 서버 문제 → 관리자 문의

---

## 3. 화면 구조

사이드바 = **4개 관리 영역 + AI 신뢰도**:
- **품질 관리** — 01 실시간 진단 / 02 불량 원인 / 03 전체 이력
- **설비 관리** — 04 예지정비(정비 우선순위)
- **안전 관리** — 05 안전 위험
- **생산 관리** — 06 생산 OEE
- **AI 신뢰도** — 07 모델 신뢰도

---

## 4. 화면별 사용법

### 화면 1 — 실시간 진단 ⭐ (가장 많이 쓰는 화면)
지금 기계가 정상인지 이상인지 보여주는 메인 화면.

- **데모 시나리오 버튼**(정상/경고/위험/긴급): 누르면 그 상황 데이터로 AI가 즉시 재판정 (교육·시연용).
- **우상단 LIVE 버튼 2개**: ▶실측 KAMP 스트림(진짜 검증 데이터 재생) / ▶데모 스트림(시나리오+노이즈). 둘은 동시에 못 켭니다.
- **KPI 4칸**: ① 상태(정상/경고/위험/긴급) ② 복원오차(AE) / τ ③ **4-AI 합의 투표**(예 4/4 → 3 이상이면 이상) ④ 강도 등급(AE 복원오차가 τ의 몇 배)
- **다중 AI 합의 미터**: AE·IF·OCSVM·LOF 각각 ▲FIRE(이상)/●HOLD(정상). 아래 박스 = "왜 4개를 쓰나" — 느슨하게 1개만 발동하면 거짓경보 31건, **3개 합의로 5건**으로 줄어듦(검증 실측).
- **24 센서 입력**: 빨간 막대 = 이상 센서, 숫자는 σ.
- **처방 카드 TOP 3**: 가장 많이 벗어난(|σ|) 센서 3개와 즉시 조치. (즉시/5분 내/관찰)
- **What-if 슬라이더**: 이상 센서를 정상 쪽으로 끌면 복원오차가 얼마나 떨어지는지 실시간 표시. 기준은 **시나리오 고정값**이라 스트리밍 중에도 안정적으로 비교됩니다.
- **자연어 진단 보고서**: AI(Claude Haiku)가 **원인 → 대처 → 리소스** 순으로 글로 설명. (σ 기호로 표기)

> 👉 출근하면 이 화면부터. 상태 "정상" + 합의 0~2/4면 안심. 빨개지면 처방 카드·보고서대로 행동.

### 화면 2 — 불량 원인 분석 (SHAP + 인과)
"왜 이상인지" 원인을 파헤치는 화면. 4개 그래프:
- **TOP-5 이상 원인 센서**: 복원오차를 키운(이상을 일으킨) **양의 SHAP** 센서를 큰 순으로. = 작업자가 손볼 범인 순위.
- **SHAP Waterfall**: 평균 정상의 기대 복원오차(기준)에서 시작 → 각 센서가 더하고 빼서 → 이 샷의 예측 복원오차까지. + 막대(빨강)=이상↑, − 막대(하늘)=정상↑. 나머지 19센서는 "기타"로 묶음.
- **인과 의존성 그래프**: SHAP 1위 센서와 **실제로 강한 상관(|r|≥0.4) + 사출 사이클 시간순서**를 만족하는 연관 센서를 연결(실측 5,357행). 단일 센서가 아닌 연쇄 원인 파악.
- **PCA 클러스터**: 24센서를 2축으로 압축한 분포 지도. 회색=정상, 빨강=불량. **추세선 아님**(두 축은 직교). 불량이 평균적으론 정상 바깥이지만 일부는 겹침 → 완벽 분리는 24차원 복원오차에서.

### 화면 3 — 전체 이력 분석
검증 데이터 **1,379샷** 전체를 한 번에 평가.
- **KPI**: 총 샷 / 탐지(TP) / 미탐(FN) / 거짓경보(FP)
- **τ 민감도 시뮬레이션**: 로그 y축 산점도. 슬라이더로 τ를 움직이면 혼동행렬이 실시간 재계산. τ=0.320에서 공식 혼동행렬(TN1334/FP6/FN13/TP26)을 그대로 재현 → 미탐↔거짓경보 트레이드오프 체험.
- **상위 20건 이상 샷**: 복원오차 큰 순. 실제 라벨(DEFECT/NORMAL)과 판정(TP/FP/FN/TN) 표시.
> 공식 혼동행렬은 중복을 피해 **화면 7**에 둡니다.

### 화면 4 — 설비 예지정비 (정비 우선순위 / 모델 커버리지)
실제 불량 39건이 **어느 설비 계통**에서 나는지(정비 우선순위) + **모델이 그 중 뭘 잡는지**(탐지 커버리지).
- **계통별 불량 + 탐지 커버리지 막대**: 하늘색=탐지(TP), 빨강=미탐(FN). 계통별 탐지율 표시.
- **모델 사각지대 진단**: 탐지율 50% 미만 계통 자동 경고. **온도 계통은 불량 11건 전부 미탐(0%)** — AE가 온도 이상에 둔감해서 못 잡습니다. 정직하게 드러내, 온도 전용 룰 보강이 필요함을 알려줍니다.
> KAMP 데이터에 시계열 가동 로그가 없어 "잔여수명(RUL) 일수"는 시연하지 않습니다(MES 연동 시점). 순서 무관한 실측 분포만 씁니다.

### 화면 5 — 안전 위험 모니터링
센서 이상을 **작업자 안전 위험**으로 변환.
- **종합 안전 등급** = 진단 결과(정상/경고/위험/긴급)와 연동.
- **과열🔥 / 과압⚠ / 기계⚙**: 각각 σ로 위험도(경고 2σ / 위험 3σ / 긴급 4.5σ).
- **안전센서 정상률**: 안전 관련 센서 중 정상(|σ|<2) 비율(실측).
- **위험성 평가 매트릭스**: 가로=심각도(현재 σ) × 세로=발생가능성(**검증 불량 39건의 실제 주원인 빈도**). 2축 모두 실측.
> 고온·고압은 곧 작업자 안전 문제 → 빨간 경보 시 **사람 안전부터**.

### 화면 6 — 생산 현황 · OEE
- **OEE** = 가동률 × 성능 × **양품률**. (양품률만 AI 실측 97.17%, 가동률·성능은 MES 연동 전이라 **가정**)
- **생산 품질 분포**: 검증 1,379샷을 12구간으로 나눠 양품(회색)+불량(빨강), y축 샷 수.
- **불량 원인 Pareto**: 불량 1건마다 가장 벗어난 센서를 집계. 상위 4개(금형온도4·사출속도·배압·충전)가 누적 95% → 이것만 집중 관리해도 불량 대부분 저감(파레토 법칙).

### 화면 7 — 모델 신뢰도 확인
"이 AI를 믿어도 되나"를 학술 검증.
- **ROC-AUC 0.9254**(분별력, 0.9 넘으면 우수), **PR-AUC 0.7080**(불균형 데이터 핵심), **F1 0.7324**, **Recall 0.667**(26/39), **Precision 0.8125**(26/32) — 모두 실측 ROC/PR 곡선.
- **혼동 행렬**: TN1334 / FP6 / FN13 / TP26.
- **합의 알고리즘 비교표**: ≥4/4·≥3/4·≥2/4·≥1/4·단독 모델·Stacking을 F1 순 비교. 성능 낮은 방식도 숨기지 않고 공개(정직성). **≥3/4가 권장**(F1 최고·거짓경보 최소·견고).
- **Cost-Sensitive Threshold**: 미탐(50만) ≫ 거짓경보(3만). 배포 τ 0.320(거짓경보 6건) → 비용·F1 최적 τ 0.517(거짓경보 3건)로 올리면 미탐 동일·총비용↓·정밀도 +8.4%p.

---

## 5. 핵심 개념 — AI가 판정하는 2단계

```
1단계 [4-AI 합의]  AE·IF·OCSVM·LOF 4개가 각자 투표 → 3개 이상 동의하면 "이상"
2단계 [강도 등급]  이상이면, AE 복원오차 / τ 비율로:
                  1.0~1.5배 = 경고 / 1.5~2.5배 = 위험 / 2.5배↑ = 긴급
```
- **복원오차**는 AE 1개의 값(얼마나 비정상인지), **판정 권한**은 4-AI 합의에 있습니다.
- **τ = (정상 복원오차 99분위수 0.1225 + F1 최적 0.5171) / 2 = 0.320.** 라벨 없이 정한 통계 기준과 라벨 최적의 절충.

---

## 6. 하루 업무 흐름

1. **출근**: 화면 1 → 상태 "정상"이면 OK.
2. **빨간 경보**: ① 자연어 보고서(원인/대처/리소스) ② 처방 카드 TOP 3 순서대로 ③ 과열·과압이면 화면 5에서 사람 안전부터 ④ 못 하는 조치는 보고서 "리소스"대로 호출.
3. **교대/마감**: 화면 6(OEE)·화면 3(이력) 확인.
4. **주간**: 화면 4(정비 우선순위) — 어느 계통을 우선 점검할지.

---

## 7. FAQ

- **숫자가 안 바뀜** → Ctrl+Shift+R. "⚠ 백엔드 미연결"이면 서버 문제.
- **보고서가 "템플릿"** → AI 글쓰기(LLM) 미연결. 핵심 수치는 정상이라 사용엔 문제없음.
- **"가정" 태그** → 데이터가 없어 가정한 값(가동률·성능 등). 참고용. 나머지는 전부 실측.
- **화면 1 처방 1위 ≠ 화면 2 SHAP 1위?** → 처방은 "가장 많이 벗어난 센서(σ)", SHAP은 "이상 판정에 가장 기여한 센서". 답하는 질문이 달라 정상입니다.
- **온도 불량은 왜 못 잡나** → AE가 온도 변동을 정상으로 학습해 복원오차가 안 커집니다(화면 4 사각지대 진단). 알려진 한계이며 온도 전용 룰로 보강 예정.

---

**끝. 막히면 1번 용어표와 6번 업무 흐름으로 돌아오세요.**
