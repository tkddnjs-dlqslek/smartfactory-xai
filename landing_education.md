# SmartFactory XAI — 대시보드 사용 교육 자료

> 대상: 품질관리 담당자 / 현장 엔지니어 / 심사위원 데모 진행자  
> 작성: 2026-04-29

---

## 대회 주제 정합성

| 평가 항목 | 우리 대시보드 | 판정 |
|---|---|---|
| 제조 현장 문제 정의 | 사출성형기 불량(1.03%) 자동 탐지 | ✅ |
| AI/데이터 기술 활용 | KAMP 공공데이터 + Autoencoder + SHAP | ✅ |
| 실시간 운영 기능 | 시뮬레이터 → 현장에서 즉시 판정 | ✅ |
| 설명 가능성 (XAI) | SHAP으로 원인 센서 즉시 출력 | ✅ |
| MVP 구현 범위 명확성 | 5탭 기능 목적 분리 | ✅ |
| 처방 시스템 | 이상 감지 시 센서별 대응 권고 | ✅ |
| 운영 시스템 전체 커버 | 탐지→진단→처방→추적 전 사이클 | ✅ |

**포지셔닝 논리 체인:**
> 이상탐지 → 불량 사전 감지 → 설비 가동률 유지 → 생산 스케줄 안정화 → 전체 공장 운영 효율화

---

## 랜딩 가이드

앱 접속 시 가이드 페이지가 먼저 표시됨.
- **"다시 보지 않기"** 체크 후 시작 → 영구 숨김 (`.guide_dismissed` 파일 생성)
- 다시 보려면: 왼쪽 사이드바(≡) → **"사용 가이드 다시 보기"**

---

## 탭별 사용법

### Tab 1 — 모델 성능 (신뢰도 확인)

> **"이 AI가 믿을 만한가?"** 를 확인하는 탭

**상단 지표 해석:**
| 지표 | 값 | 의미 |
|---|---|---|
| ROC-AUC | 0.9254 | 정상/불량 구분 능력. 0.80 이상 = 실사용 가능 |
| Recall | 0.6667 | 실제 불량 10건 중 6~7건 탐지 |
| Precision | 0.8125 | "이상" 알림 중 81%가 실제 불량 |
| F1-Score | 0.7324 | Recall과 Precision의 균형 지표 |

**차트 해석:**
- **복원 오차 분포**: 회색(정상)과 빨간(불량)이 많이 분리될수록 좋음
- **ROC 곡선**: 선이 좌상단에 붙을수록 좋음 (점선 = 랜덤 수준)
- **Confusion Matrix**: 우상단 숫자(불량 → 정상 분류) 최소화가 목표

---

### Tab 2 — 실시간 시뮬레이터 (현장 활용 핵심)

> **"지금 이 공정 값, 이상인가? 원인은 뭔가?"** 즉시 판정

**사용 순서:**

1. 왼쪽 슬라이더로 현장 측정값 입력 (그룹 클릭 → 펼쳐짐)
   - 기본값 = 훈련 데이터 평균 (정상 공정 중심)
   - 슬라이더 양 끝 = ±4σ 극단값

2. 오른쪽 패널 실시간 확인
   - **✓ 정상 (NORMAL)**: 흰 배너 + 게이지 안전 구간
   - **⚠ 이상 감지 (ANOMALY)**: 빨간 배너 + 게이지 초과 + 센서별 권고 사항 자동 출력

3. 이상 시 **"SHAP 원인 분석 실행"** (약 10초)
   - 빨간 막대 = 이상 기여 센서 (길수록 주원인)
   - 회색 막대 = 정상 기여 센서

**시연 팁:** Filling_Time 슬라이더를 최대로 올리면 즉시 이상 판정 → 권고 사항 표시

---

### Tab 3 — 대규모 스코어링 (배치 분석)

> **"35,000개 과거 공정 중 언제 이상이 집중됐나?"**

1. 상단 지표에서 전체 이상률 확인 (기본 ~17%)
2. **임계값 슬라이더** 조정:
   - 낮춤 → 더 많이 잡힘 (오탐 증가, 불량 비용이 클 때 사용)
   - 높임 → 덜 잡힘 (미탐 증가, 오탐 비용이 클 때 사용)
3. 시계열 차트에서 **빨간 점이 뭉친 구간** = 집중 점검 필요 시점
   - 하단 범위 슬라이더로 특정 구간 확대 가능
4. 하단 테이블: 오차 상위 20개 샷 원시값 확인

---

### Tab 4 — XAI 원인 분석 (근본 원인)

> **"어떤 센서가 불량의 원인인가?"** 글로벌·샘플별 분석

**글로벌 분석 (왼쪽 차트):**
- 빨간 막대 상위 5개 = 반복적으로 불량을 일으킨 센서
- 현재 결과: Filling_Time > Injection_Time > Max_Switch_Over_Pressure
- → "충전 시간과 사출 시간이 공정 불량의 핵심 원인"

**개별 샘플 Waterfall (아래):**
- 슬라이더로 샘플 선택
- 빨간 막대 = 해당 샷의 이상 원인 센서
- 테이블에서 원시값 역추적 가능

---

### Tab 5 — 생산 이력 (추세 & 건강도)

> **"어느 구간에서 불량이 몰렸나? 장비 상태는?"**

1. **집계 단위** 선택 (500샷 권장)
2. 상단 빨간 막대 = 구간별 이상률, 튀는 구간 집중 점검
3. 하단 꺾은선 = 평균 복원 오차, 임계값(빨간 점선) 위 구간 확인
4. **장비 건강도 스코어** 확인 (최근 이상률 기반)
5. **이상 집중 구간 Top 10** 테이블 = 현장 회의 자료로 직접 사용

---

## 5분 데모 시나리오

```
Tab 1 (30초)  → "AI 모델 신뢰도 확인 — ROC-AUC 0.9254"
Tab 3 (1분)   → "35K 과거 데이터에서 이상 집중 구간 발견"
Tab 5 (1분)   → "구간별 이상률 추이, 장비 건강도 확인"
Tab 2 (2분)   → "의심 센서값 입력 → 즉시 판정 + 대응 권고"
Tab 4 (30초)  → "SHAP으로 불량 원인 센서 설명"
```

---

## 핵심 메시지 (심사위원용)

> "단순 이상탐지가 아닙니다.  
> **탐지(Detect) → 진단(Diagnose) → 처방(Prescribe) → 추적(Track)**  
> 4단계 공정 운영 의사결정 지원 시스템입니다."
