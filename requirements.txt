# ── 재현성 고정 버전 (pip freeze 기준, Python 3.11) ──
torch==2.3.1
numpy==1.26.4
pandas==2.1.4
scikit-learn==1.2.2
shap==0.48.0
matplotlib==3.8.3
seaborn==0.13.2
plotly==5.22.0
streamlit==1.36.0
pyarrow==15.0.2
joblib==1.4.2
tqdm==4.66.4
python-pptx==1.0.2

# 재현성 설정: random_state=42, numpy.seed=42, torch.manual_seed(42)
# GPU 환경: torch.backends.cudnn.deterministic=True, torch.backends.cudnn.benchmark=False
# 실행 순서: 01_train.py → 02_evaluate.py → 03_score_unlabeled.py → 04_compute_shap.py → 06_bootstrap_ci.py → streamlit run app.py
