# SmartFactory XAI — 발표 대본

> 분량: 약 7~9분 / 슬라이드 13장 기준. 각 절 머리의 **[슬라이드 N]**과 슬라이드 프롬프트 번호가 1:1 대응합니다.
> 톤: 차분하게, 숫자는 또박또박. ★는 강조 포인트.

---

## [슬라이드 1] 표지 — 문제 제기 (40초)

안녕하십니까. 저희는 사출성형 공정의 불량을, 터지기 전에 잡고 **왜 그런지까지 설명하는** 통합 운영 플랫폼 **SmartFactory XAI**를 만들었습니다.

현장의 진짜 문제는 두 가지입니다. 첫째, **불량은 사후에 발견**됩니다 — 작업자 육안 검사로는 이미 다량 생산된 뒤에야 잡힙니다. 둘째, AI가 "불량"이라고 해도 **왜 그런지 설명을 못 하면** 현장은 믿고 행동하지 못합니다. 저희는 이 둘을 **이상 탐지 + 설명가능 AI(XAI)**로 동시에 풉니다.

---

## [슬라이드 2] 데이터 — KAMP 사출성형 (50초)

데이터는 공공데이터포털 KAMP의 사출성형기 데이터를 썼습니다. cn7 차종 부품 라인이고, **24개 공정 센서**를 봅니다 — 시간·위치·속도·압력·온도 5개 계열입니다.

핵심은 **극심한 불균형**입니다. 라벨된 데이터에서 불량은 전체의 약 1%뿐입니다. ★ 불량 사례가 이렇게 적으면 "불량을 학습"하는 보통의 지도학습은 불가능합니다. 그래서 발상을 뒤집었습니다.

---

## [슬라이드 3] EDA — 무엇을 봤나 (50초)

먼저 데이터를 들여다봤습니다(EDA). 24개 센서의 분포와 상관관계, 그리고 불량 라벨과의 관계를 확인했습니다.

세 가지를 발견했습니다. 첫째, **정상 데이터는 좁은 범위에 모여** 있고 불량은 그 바깥으로 튑니다 — 이상 탐지가 통할 신호입니다. 둘째, 센서끼리 **사출 사이클 순서를 따라 강하게 상관**합니다(예: 사출압력↔형체결 r=0.99) — 나중에 인과 그래프의 근거가 됩니다. 셋째, 단위·범위가 제각각이라 **표준화(z-score)가 필수**입니다.

---

## [슬라이드 4] 핵심 발상 — 준지도 이상 탐지 (50초)

불량이 1%뿐이니, **정상만 학습**합니다. 이것이 준지도 이상 탐지입니다.

오토인코더라는 모델에게 "정상 샷이 어떻게 생겼는지"만 압축했다 복원하도록 가르칩니다. 그러면 ★ **정상은 잘 복원하고(오차 작음), 처음 보는 이상은 복원에 실패(오차 큼)**합니다. 이 **복원 오차**가 이상의 척도입니다. 불량을 한 건도 안 봐도 이상을 잡을 수 있습니다.

---

## [슬라이드 5] 파이프라인 — 데이터에서 판정까지 (60초)

전체 과정은 이렇습니다.

① **전처리**: 24센서를 StandardScaler로 z-score 정규화.
② **학습**: 정상 데이터로 오토인코더를 학습합니다 — 구조는 24→16→8→16→24, 8차원으로 압축했다 복원. MSE 손실, Adam, 조기종료. 베이스라인 모델(Isolation Forest·One-Class SVM·LOF)도 같은 정상 데이터로 학습합니다.
③ **임계값 결정**: 정상 복원오차의 99분위수(0.1225)와 F1 최적값(0.5171)의 **평균 0.320**을 임계값 τ로 잡았습니다. 라벨 없이 정한 통계 기준과, 라벨로 최적화한 값의 절충입니다.
④ **추론**: 새 샷이 들어오면 4개 모델이 각자 판정합니다.
⑤ **설명**: SHAP로 원인 센서를, 인과 그래프로 연쇄 관계를 뽑습니다.
⑥ **보고**: Claude Haiku가 자연어 보고서를 씁니다.

---

## [슬라이드 6] AI가 판정하는 법 — 2단계 (70초) ★핵심

여기가 핵심입니다. 저희는 **모델 1개를 믿지 않습니다.**

**1단계 — 4-AI 합의.** 성격이 다른 4개 모델(오토인코더·Isolation Forest·One-Class SVM·LOF)이 각자 "이상이다/아니다" 투표합니다. ★ **3개 이상이 동의해야 이상**으로 판정합니다. 서로 다른 약점을 가진 모델들이라, 한 모델이 헷갈리는 정상 패턴을 다른 모델이 걸러줍니다. 실제로 — 느슨하게 1개만 발동시키면 거짓경보가 31건인데, **3개 합의로 바꾸면 5건**으로 줄고 정밀도는 0.50에서 0.84로 오릅니다.

**2단계 — 강도 등급.** 이상으로 판정되면, 오토인코더 복원오차가 임계값의 몇 배인지로 **경고·위험·긴급**을 나눕니다. 1.5배 미만 경고, 2.5배 미만 위험, 그 이상 긴급입니다.

정리하면 — **판정 권한은 4-AI 합의에, 강도 눈금은 복원오차에** 있습니다.

---

## [슬라이드 7] 설명가능 AI — 왜 그런지 (60초)

판정만으로는 부족합니다. 현장은 "어느 센서가 범인이냐"를 알아야 합니다.

**SHAP**로 각 센서가 이상 판정에 **얼마나 기여**했는지 계산합니다. ★ 복원오차를 키운 쪽, 즉 **양의 기여 센서가 진짜 원인**입니다. 이걸 큰 순으로 보여줍니다.

**인과 그래프**는 한 발 더 나갑니다. 원인 센서와 실제로 강하게 상관하고(|r|≥0.4), 사출 사이클상 먼저 일어나는 센서를 연결해 **연쇄 원인**을 보여줍니다 — 단일 센서가 아니라 "상류 공정이 진짜 원인일 수 있다"를 짚습니다.

마지막으로 **Claude Haiku**가 이 결과를 **원인 → 대처 → 리소스** 3단으로 자연어 보고서를 씁니다. 작업자가 바로 행동할 수 있게요.

---

## [슬라이드 8] 성능 — 믿을 만한가 (50초)

검증셋 1,379샷(정상 1,340 + 불량 39)으로 평가했습니다.

★ **ROC-AUC 0.9254** — 분별력이 우수합니다(0.9 이상). 불균형 데이터의 핵심 지표인 **PR-AUC는 0.708**, **F1은 0.732**입니다. 불량 39건 중 26건을 잡았고(재현율 0.667), 이상이라 한 것 중 81%가 진짜 불량입니다(정밀도 0.813).

저희는 성능 낮은 판정 방식도 **비교표에 숨김없이 공개**합니다. 신뢰는 정직에서 나온다고 봤습니다.

---

## [슬라이드 9] 정직성 — 우리 모델의 한계 (60초) ★차별점

여기가 저희의 차별점입니다. 저희는 **모델의 약점까지 화면으로 드러냅니다.**

불량을 계통별로 보면, 실제 불량 1위 원인은 **금형온도(11건)**입니다. 그런데 ★ **우리 모델은 이 온도 불량을 한 건도 못 잡습니다(탐지율 0%).** 놓친 불량 13건 중 11건이 온도입니다.

이유는 분명합니다 — 오토인코더가 온도 변동을 정상으로 학습해서, 온도가 벗어나도 복원오차가 안 커집니다. 임계값을 낮춰도 정상 잡음과 구분이 안 됩니다.

이걸 숨기지 않고, **"모델 사각지대 진단" 화면이 자동으로 잡아내** 온도 전용 룰 보강이 필요하다고 알려줍니다. ★ 자기 한계를 아는 AI가, 모르는 척하는 AI보다 현장에서 안전합니다.

---

## [슬라이드 10] 통합 플랫폼 — 4축 (45초)

이 엔진을 4개 운영 영역으로 확장했습니다.

- **품질**: 실시간 진단·원인분석·전체 이력
- **설비**: 계통별 정비 우선순위와 모델 커버리지
- **안전**: 센서 이상을 과열·과압·기계 위험으로 변환, 위험성 평가 매트릭스
- **생산**: OEE와 불량 Pareto

같은 이상탐지 결과를, **보는 사람(작업자·반장·부서장)과 목적(품질·안전·생산)에 맞게** 재해석합니다.

---

## [슬라이드 11] 라이브 데모 (60초)

(화면 전환) 실시간 진단 화면입니다. **긴급 시나리오**를 누르면 — 4개 AI가 모두 FIRE, 상태 긴급, 복원오차가 임계값의 10배로 뜹니다.

원인 분석으로 가면 — **사출속도가 SHAP 1위**, 워터폴이 기준값에서 어떻게 이 값까지 쌓였는지 분해해 보여줍니다. 자연어 보고서는 "최대 배압 +8.4σ, 배압 설정 확인, 설비보전팀 호출"이라고 바로 행동을 지시합니다.

What-if 슬라이더로 사출속도를 정상 쪽으로 끌면 — 복원오차가 실시간으로 떨어집니다. "이 센서만 잡으면 된다"를 즉시 확인합니다.

---

## [슬라이드 12] 기술 스택 & 정합성 (35초)

프론트는 Next.js, 백엔드는 FastAPI로 **실제 모델을 실시간 추론**합니다. 목업이 아니라, 화면의 모든 그래프가 검증 1,379샷 실측에서 계산됩니다. 7개 화면의 모든 수치가 **단일 검증셋 기준으로 정합**하도록 교차검증했습니다 — ROC, 혼동행렬, Pareto, 커버리지가 서로 어긋나지 않습니다.

---

## [슬라이드 13] 마무리 (30초)

정리하겠습니다. SmartFactory XAI는 — ① **불량 1%의 불균형을 준지도 이상탐지로** 풀고, ② **4-AI 합의로 거짓경보를 줄이며**, ③ **SHAP·인과·자연어로 왜 그런지 설명**하고, ④ **자기 한계까지 정직하게 드러내는** 통합 운영 플랫폼입니다.

설명할 수 있고, 정직한 AI. 그래서 현장이 믿고 쓸 수 있습니다. 감사합니다.

---

## 예상 질문 대비 (Q&A)

- **Q. 온도를 못 잡으면 실용성이 떨어지지 않나?**
  → 알려진 한계이고, 화면이 자동으로 그 사각지대를 짚어줍니다. 온도는 복원오차 대신 단순 임계 룰로 보강하면 즉시 커버됩니다(공정 온도는 SPC 관리도가 잘 통하는 영역). 모델·룰 하이브리드가 다음 단계입니다.
- **Q. 4-AI가 과한 것 아닌가? AE 하나로는?**
  → AE 단독은 거짓경보가 많고, IF·OCSVM·LOF는 서로 다른 정상 가정을 보완합니다. 합의로 정밀도 0.50→0.84, 거짓경보 31→5건. 견고성도 올라갑니다.
- **Q. RUL(잔여수명)은 왜 없나?**
  → cn7 데이터에 시계열 가동 로그가 없어 진짜 RUL은 시연 불가입니다. 가짜 외삽 대신, 순서 무관한 실측(계통별 정비 우선순위)만 정직하게 보여줍니다. MES 연동 시 RUL이 활성화됩니다.
- **Q. τ는 어떻게 정했나?**
  → 정상 복원오차 99분위수(0.1225)와 F1 최적값(0.5171)의 평균(0.320). 운영 시엔 불량 라벨이 없으니 통계 기준을, 검증 땐 F1을 반영한 절충입니다.
- **Q. 가동률·성능이 가정값인데 OEE가 의미 있나?**
  → 양품률은 우리 AI가 실측합니다(97.17%). 가동률·성능은 MES 가동로그 연동 지점이라 가정으로 명시했습니다 — 숨기지 않고 "연동하면 자동 실측"임을 표기합니다.
