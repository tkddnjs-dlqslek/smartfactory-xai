# SmartFactory XAI — 발표 대본 (상세판 · 평가기준 정렬)

> 이 대본은 **공식 평가기준 100점**에 맞춰 각 절이 어떤 항목을 겨냥하는지 표시했습니다.
> 분량은 "넣을 수 있는 내용을 다 넣은" 상세판입니다 — **굵게(★)**가 꼭 말할 핵심, 그 외는 시간 보고 가지치기하세요.
> 권장 길이: 발표 6~8분(슬라이드 13장) + Q&A. `[항목]`은 평가 배점 매핑.

## 평가기준 → 슬라이드 매핑 (채점자 시점)

| 평가 항목 | 배점 | 주력 슬라이드 | 핵심 메시지 |
|---|---|---|---|
| 문제 정의 | 20 | 1·2 | 사출성형 라인의 "사후 발견 + 설명 부재" 문제, 적용 대상·목적 명확 |
| AI 활용 | 25 | 4·5·6·7 | 불량 1% 불균형 → 준지도 이상탐지 / 4-AI 합의 / SHAP·인과·NLG로 실질 기여 |
| 플랫폼 기획 | 20 | 5·10 | 데이터 흐름(센서→판정→설명→4축)과 사용자 흐름이 유기적으로 연결 |
| MVP 완성도 | 25 | 8·11·12 | 실제 모델 실시간 추론, 7탭 시연 가능, 전부 실측 정합 |
| 발표/전달력 | 10 | 전체+Q&A | 시간 준수, 기획 의도·구현을 설득력 있게 |

---

## [슬라이드 1] 표지 / 문제 정의 (45초) `[문제 정의 20]`

안녕하십니까. 저희 주제는 사출성형 공정의 불량을 **터지기 전에 잡고, 왜 그런지까지 설명하는** 통합 운영 플랫폼 **SmartFactory XAI**입니다.

★ **적용 대상**은 자동차 부품 사출성형 라인(KAMP cn7), **활용 목적**은 "불량 사전 탐지 + 원인 설명 + 즉시 처방"입니다.

현장의 진짜 문제는 두 가지입니다.
- ★ 첫째, **불량이 사후에 발견**됩니다. 작업자 육안 검사로는 이미 다량 생산된 뒤에야 잡혀, 폐기·재작업 비용이 큽니다.
- ★ 둘째, AI가 "불량"이라고 해도 **왜 그런지 설명을 못 하면** 현장은 믿고 행동하지 못합니다 — 이른바 블랙박스 문제.

저희는 이 둘을 **이상 탐지 + 설명가능 AI(XAI)**로 동시에 풉니다.

*(추가 가능)* 사출성형은 온도·압력·속도가 미세하게 어긋나도 불량이 나는데, 변수가 24개라 사람이 동시에 감시하기 어렵습니다.

---

## [슬라이드 2] 데이터 & 문제의 본질 — 불균형 (55초) `[문제 정의 20]`

데이터는 공공데이터포털 **KAMP 사출성형기 데이터**(cn7 차종)입니다. **24개 공정 센서**를 봅니다 — 시간·위치·속도·압력·온도 5개 계열입니다.

★ 핵심은 **극심한 불균형**입니다. 라벨된 데이터에서 불량은 전체의 **약 1%**뿐입니다. 검증셋 기준으로도 1,379샷 중 정상 1,340, 불량 39건입니다.

★ 불량 사례가 이렇게 적으면, "불량을 학습"하는 보통의 지도학습은 **불가능**합니다. 불량 패턴을 충분히 못 배우니까요. 그래서 저희는 **발상을 뒤집었습니다** — 다음 장에서.

*(추가 가능)* 라벨 데이터 약 6,700행 + 비라벨 운영 데이터 약 80만 행. 불량률이 워낙 낮아 "정확도 99%"는 의미가 없습니다(다 정상이라 찍어도 99%). 그래서 재현율·정밀도·PR-AUC를 봅니다.

---

## [슬라이드 3] EDA — 데이터에서 무엇을 봤나 (70초) `[문제 정의 20 / AI 활용 25]`

모델을 만들기 전에 데이터를 충분히 들여다봤습니다(EDA). 24개 센서의 **분포·상관·불량과의 관계**를 확인해 ★ **세 가지를 발견**했고, 이게 모델 설계의 근거가 됐습니다.

★ **첫째 — 정상은 좁게 모이고 불량은 바깥으로 튄다.** 센서값 분포를 보면 정상 샷들은 좁은 범위에 빽빽이 모여 있고, 불량은 그 군집 밖으로 벗어납니다. → ★ "정상의 모습"만 배워두면 거기서 벗어나는 걸로 이상을 잡을 수 있다 = **이상 탐지가 통할 신호**. 이게 모델 선택의 첫 근거입니다.

★ **둘째 — 센서끼리 사출 사이클 순서를 따라 강하게 상관한다.** 예를 들어 **사출압력과 형체결 시간은 상관계수 r=0.99**, 쿠션 위치-형개방 0.99 등, 한 공정이 다음 공정에 영향을 줍니다. → 이건 나중에 **인과 그래프**의 근거가 됩니다(단일 센서가 아니라 연쇄 원인).

★ **셋째 — 센서마다 단위·범위가 제각각이다.** 온도는 200~300, 시간은 수 초, 압력은 수천까지. 그대로 넣으면 큰 숫자 센서가 모델을 지배합니다. → ★ **표준화(z-score) 정규화가 필수**. 각 센서를 "평소 대비 몇 σ 벗어났나"로 변환합니다.

*(추가 가능)*
- 학습/검증 분리: 정상 데이터의 대부분으로 학습, 일부 정상 + 불량 전체로 검증.
- 불량 39건의 주원인 센서를 미리 집계해보니 온도·속도·압력·시간에 분산 — 특정 한 센서가 아니라 여러 계통에서 불량이 발생.
- 위치 계열은 정상·불량 모두 변동 폭이 비슷해 불량 주원인으로 거의 안 나옴(검증 데이터 기준).

---

## [슬라이드 4] 핵심 발상 — 준지도 이상 탐지 (55초) `[AI 활용 25]`

불량이 1%뿐이니, ★ **불량을 학습하지 않고 "정상만" 학습**합니다. 이것이 **준지도 이상 탐지(semi-supervised anomaly detection)**입니다.

핵심 모델은 **오토인코더(Autoencoder)**입니다. 정상 샷을 입력받아 8차원으로 압축했다가 다시 24차원으로 복원하도록 학습시킵니다. "정상이 어떻게 생겼는지"를 압축 표현으로 외우는 겁니다.

★ 그러면 — **정상 입력은 잘 복원해서 오차가 작고, 처음 보는 이상 입력은 복원에 실패해서 오차가 큽니다.** 이 **복원 오차(reconstruction error)**가 곧 이상의 척도입니다.

★ 결정적 장점: **불량을 한 건도 안 봐도 이상을 잡을 수 있습니다.** 불량 데이터가 부족한 현장 문제 특성에 정확히 맞는 접근입니다.

*(추가 가능)* 구조는 24→16→8→16→24, MSE 손실, Adam, 조기종료. 8차원 병목(bottleneck)이 "정상의 핵심"만 남기는 필터 역할을 합니다.

---

## [슬라이드 5] 파이프라인 — 데이터에서 판정·설명까지 (65초) `[AI 활용 25 / 플랫폼 기획 20]`

전체 데이터 흐름은 6단계입니다. ★ 이 흐름이 끊김 없이 연결되는 게 플랫폼의 뼈대입니다.

① **전처리** — 24센서를 z-score 정규화.
② **학습** — 정상 데이터로 오토인코더 + 베이스라인 3종(Isolation Forest·One-Class SVM·LOF)을 모두 학습.
③ **임계값 결정** — 정상 복원오차의 99분위수(0.1225)와 F1 최적값(0.5171)의 **평균 0.320**을 임계값 τ로. 라벨 없이 정한 통계 기준과 라벨 최적값의 절충입니다.
④ **추론** — 새 샷이 들어오면 4개 모델이 각자 판정 → 합의.
⑤ **설명** — SHAP로 원인 센서, 인과 그래프로 연쇄 관계.
⑥ **보고** — Claude Haiku가 자연어 보고서 작성.

★ 센서 입력 → 판정 → 원인 → 처방까지 **하나의 파이프라인**으로 흐르고, 그 결과를 4개 운영 화면이 나눠 씁니다.

---

## [슬라이드 6] AI가 판정하는 법 — 2단계 (75초) ★최핵심 `[AI 활용 25]`

여기가 AI 활용의 핵심입니다. ★ **저희는 모델 1개를 믿지 않습니다.**

★ **1단계 — 4-AI 합의.** 성격이 다른 4개 모델(오토인코더·Isolation Forest·One-Class SVM·LOF)이 각자 "이상이다/아니다" 투표합니다. **3개 이상 동의해야 이상**으로 판정합니다. 서로 다른 약점을 가진 모델이라, 한 모델이 헷갈리는 정상 패턴을 다른 모델이 걸러줍니다.

★ 효과는 실측으로 증명됩니다 — 느슨하게 1개만 발동시키면 거짓경보가 **31건**인데, **3개 합의로 바꾸면 5건**으로 줄고 정밀도는 **0.50 → 0.84**로 오릅니다. 거짓경보를 줄이면서 견고해집니다.

★ **2단계 — 강도 등급.** 이상으로 판정되면, 오토인코더 복원오차가 임계값의 몇 배인지로 **경고·위험·긴급**을 나눕니다(1.5배 미만 경고 / 2.5배 미만 위험 / 그 이상 긴급).

★ 정리하면 — **판정 권한은 4-AI 합의에, 강도 눈금은 복원오차에** 있습니다. 복원오차는 "얼마나 비정상인가", 합의는 "진짜 이상인가"를 답합니다.

---

## [슬라이드 7] 설명가능 AI — 왜 그런지 (65초) `[AI 활용 25]`

판정만으로는 현장이 안 움직입니다. "어느 센서가 범인이냐"를 알아야 합니다.

★ **SHAP** — 각 센서가 이상 판정에 얼마나 기여했는지 계산합니다. ★ 복원오차를 **키운 쪽, 즉 양의 기여 센서가 진짜 원인**입니다. 이걸 큰 순으로 보여줍니다. (음의 기여는 "정상처럼 보이게 한" 반대 증거라 원인에서 제외)

★ **인과 그래프** — 한 발 더 나갑니다. EDA에서 본 센서 간 강한 상관(|r|≥0.4)과 사출 사이클 시간 순서를 결합해, 원인 센서의 **연쇄 관계**를 보여줍니다. "이 센서가 단독 범인이 아니라 상류 공정이 진짜 원인일 수 있다"를 짚습니다.

★ **자연어 보고서 (Claude Haiku)** — 이 결과를 **원인 → 대처 → 리소스** 3단으로 글로 씁니다. 예: "최대 배압이 +8.4σ 높음 → HMI에서 배압 설정 확인 → 설비보전팀 호출". 작업자가 바로 행동할 수 있습니다.

*(추가 가능)* 같은 보고서를 작업자·반장·부서장 눈높이로 다시 써줍니다.

---

## [슬라이드 8] 성능 — 믿을 만한가 (55초) `[MVP 완성도 25]`

검증셋 1,379샷(정상 1,340 + 불량 39)으로 평가했습니다.

★ **ROC-AUC 0.9254** — 분별력이 우수합니다(0.9 이상). 불균형 데이터의 핵심 지표인 **PR-AUC 0.708**, **F1 0.732**. 불량 39건 중 26건을 잡았고(**재현율 0.667**), 이상이라 한 것 중 **81%가 진짜 불량**(정밀도 0.813)입니다. 혼동행렬은 정상 1,334개 정탐, 거짓경보 6건, 미탐 13건, 정탐 26건.

★ 그리고 — 저희는 성능 낮은 판정 방식(만장일치·다수결·단독 모델 등)도 **비교표에 숨김없이 공개**합니다. 신뢰는 정직에서 나온다고 봤습니다.

---

## [슬라이드 9] 정직성 — 우리 모델의 한계 (65초) ★차별점 `[AI 활용 25 / 발표 10]`

여기가 저희의 차별점입니다. ★ **저희는 모델의 약점까지 화면으로 드러냅니다.**

불량을 설비 계통별로 보면, 실제 불량 1위 원인은 **온도(금형온도, 11건)**입니다. 그런데 ★ **우리 모델은 이 온도 불량을 거의 못 잡습니다.**

★ 실측으로 분해해보면 — 온도 불량 11건을 오토인코더는 0건, Isolation Forest 4건, One-Class SVM 2건, LOF 1건, **4-AI 합의로는 단 1건**만 잡습니다. 즉 ★ **합의 방식 문제가 아니라, 4개 모델 전부가 온도 신호에 둔감**합니다. 온도는 정상일 때도 변동 폭이 커서, 불량 시 편차(2σ 수준)가 정상 변동에 묻히기 때문입니다.

★ 핵심은 — 이걸 숨기지 않고, **"모델 사각지대 진단" 화면이 자동으로 잡아내** "온도 계통은 별도 룰/SPC 관리도로 보강하라"고 알려줍니다.

★ **자기 한계를 아는 AI가, 모르는 척하는 AI보다 현장에서 안전합니다.** 이 정직성이 저희 플랫폼의 신뢰 기반입니다.

---

## [슬라이드 10] 통합 플랫폼 — 4축 + 사용자 흐름 (55초) `[플랫폼 기획 20]`

이 하나의 이상탐지 엔진을 **4개 운영 영역**으로 확장했습니다.

- **품질** — 실시간 진단·원인분석·전체 이력
- **설비** — 계통별 정비 우선순위 + 모델 탐지 커버리지
- **안전** — 센서 이상을 과열·과압·기계 위험으로 변환, ISO 12100 위험성 평가
- **생산** — OEE와 불량 Pareto

★ 핵심은 **같은 판정 결과를 보는 사람·목적에 맞게 재해석**한다는 점입니다. ★ 사용자 흐름도 자연스럽습니다 — 출근하면 ①실시간 진단 → 경보 뜨면 ②원인 분석 → 안전 위험이면 ⑤안전 → 마감엔 ⑥생산·③이력 → 주간엔 ④정비. 데이터 흐름과 업무 흐름이 한 줄로 연결됩니다.

---

## [슬라이드 11] 라이브 데모 (70초) ★ `[MVP 완성도 25]`

(화면 전환) ★ **실제로 동작합니다. 목업이 아닙니다.**

실시간 진단 화면에서 **긴급 시나리오**를 누르면 — 4개 AI가 모두 발동(FIRE), 상태 긴급, 복원오차가 임계값의 약 10배로 뜹니다.

원인 분석으로 가면 — **사출속도가 SHAP 1위(+1.43)**, 워터폴이 기준값(평균 정상 오차)에서 각 센서가 얼마씩 더해져 이 값까지 왔는지 분해합니다. 자연어 보고서가 "배압 +8.4σ, 배압 설정 확인, 설비보전팀 호출"로 즉시 행동을 지시합니다.

★ **What-if 슬라이더**로 사출속도를 정상 쪽으로 끌면 — 복원오차가 실시간으로 떨어집니다. "이 센서만 잡으면 된다"를 즉석에서 확인합니다.

*(추가 가능)* LIVE 스트림 버튼으로 검증 1,379샷을 실제로 한 개씩 재생하며 실시간 누적 불량률도 보여줍니다.

---

## [슬라이드 12] 기술 스택 & 데이터 정합성 (40초) `[MVP 완성도 25]`

프론트는 **Next.js**, 백엔드는 **FastAPI**로 ★ **PyTorch 모델을 실시간 추론**합니다. SHAP·Claude Haiku도 라이브로 호출됩니다.

★ 화면의 **모든 그래프가 검증 1,379샷 실측에서 계산**됩니다 — 하드코딩 목업이 아닙니다. ROC·혼동행렬·Pareto·커버리지·PCA가 **단일 검증셋 기준으로 서로 어긋나지 않도록 7개 화면을 교차검증**했습니다. (값이 없는 가동률·성능만 "가정" 태그로 명시)

---

## [슬라이드 13] 마무리 (35초) `[발표/전달력 10]`

정리하겠습니다. SmartFactory XAI는 —
① **불량 1%의 불균형을 준지도 이상탐지로** 풀고,
② **4-AI 합의로 거짓경보를 31건→5건으로 줄이며**,
③ **SHAP·인과·자연어로 "왜 그런지"를 설명**하고,
④ **자기 한계(온도 사각지대)까지 정직하게 드러내는** 통합 운영 플랫폼입니다.

★ **설명할 수 있고, 정직한 AI. 그래서 현장이 믿고 쓸 수 있습니다.** 감사합니다.

---

## 예상 질문 대비 (Q&A) `[발표/전달력 10]`

- **Q. 온도를 못 잡으면 실용성이 떨어지지 않나?**
  → 알려진 한계이고, 화면이 자동으로 그 사각지대를 짚어줍니다. 온도는 변동 폭이 커서 복원오차 기반엔 안 맞고, 단순 임계 룰(SPC 관리도)로 보강하면 즉시 커버됩니다. 모델+룰 하이브리드가 다음 단계입니다.
- **Q. 4-AI가 과하지 않나? 오토인코더 하나로는?**
  → AE 단독은 거짓경보가 많습니다. IF·OCSVM·LOF는 서로 다른 "정상 가정"을 보완합니다. 합의로 정밀도 0.50→0.84, 거짓경보 31→5건. 견고성도 올라갑니다.
- **Q. 온도 미탐은 합의를 3개로 잡아서 아닌가?**
  → 아닙니다. 합의를 1개로 풀어도 각 모델이 0~4건만 잡아 절반도 못 건집니다. 투표 방식이 아니라 4모델 전부가 온도에 둔감한 게 원인입니다.
- **Q. τ는 어떻게 정했나?**
  → 정상 복원오차 99분위수(0.1225)와 F1 최적값(0.5171)의 평균(0.320). 운영 시엔 불량 라벨이 없으니 통계 기준을, 검증 땐 F1을 반영한 절충입니다.
- **Q. OEE의 가동률·성능이 가정인데 의미 있나?**
  → 양품률은 우리 AI가 실측(97.17%)합니다. 가동률(=실가동시간/부하시간), 성능(=이론사이클타임×생산량/실가동시간)은 MES 가동·정지 로그가 필요해 가정으로 명시했습니다. 연동하면 자동 실측됩니다.
- **Q. 비라벨 80만 행은 왜 안 쓰나?**
  → 그 데이터는 cn7과 분포가 달라(설비·부품 상이) 우리 모델엔 분포 밖(OOD)이라 적용 시 대부분 이상으로 찍힙니다. 정직하게, 라벨이 있는 cn7 검증셋(1,379) 기준으로만 성능을 보고합니다.
- **Q. SHAP의 부호는?**
  → 양수 = 복원오차를 키움 = 이상의 원인. 음수 = 정상 쪽으로 당김 = 반대 증거. 그래서 원인 분석엔 양의 SHAP만 큰 순으로 봅니다.

---

## 시간 배분 가이드 (총 ≈ 7분)
| 구간 | 슬라이드 | 시간 |
|---|---|---|
| 문제 정의 | 1·2 | ~1:40 |
| AI 활용(EDA·발상·파이프라인·판정·XAI) | 3~7 | ~5:00 누적 전 핵심 |
| 성능·정직성 | 8·9 | +2:00 |
| 플랫폼·데모·정합성 | 10~12 | +2:40 |
| 마무리 | 13 | 마지막 35초 |
> 시간 부족 시 가지치기 우선순위: 슬라이드 3의 *(추가)* → 12 → 10 추가 멘트. **6·9는 절대 줄이지 마세요(차별점).**
