# 슬라이드용 검증 데이터 (단일 진실 소스 · SSOT)

> ⚠️ **절대 규칙**: 아래 값들은 `results/slide_data.json`(검증 1,379샷 실측)에서 직접 추출한 **확정 수치**입니다.
> 슬라이드/차트를 만들 때 이 값을 **그대로** 쓰고, **임의로 바꾸거나 반올림하지 마세요.** 새 숫자를 지어내지 마세요.
> 원본: `results/slide_data.json` (이 파일과 항상 일치).

---

## 1. 헤드라인 수치 (그대로 박을 것)

| 항목 | 값 | 비고 |
|---|---|---|
| 센서 수 | **24** | 5계열(시간·위치·속도·압력·온도) |
| 검증 샷 | **1,379** | 정상 **1,340** + 불량 **39** |
| 불량률 | **약 1%** | (라벨 데이터 기준) |
| ROC-AUC | **0.9254** | 분별력 (0.9↑ 우수) |
| PR-AUC | **0.7080** | 불균형 핵심지표 |
| F1-Score | **0.7324** | |
| Recall(재현율) | **0.6667** | = 26/39 |
| Precision(정밀도) | **0.8125** | = 26/32 |
| 임계값 τ | **0.3198** (≈0.320) | = (99분위 0.1225 + F1최적 0.5171)/2 |

**혼동행렬**: TN **1334** / FP **6** / FN **13** / TP **26**

**4-AI 거짓경보 감소**: ≥1/4 발동 FP **31**(정밀도 0.50) → ≥3/4 합의 FP **5**(정밀도 0.84)

**비용 임계**: 배포 τ 0.3198(거짓경보 6건·668만원) → 비용·F1최적 τ 0.5171(거짓경보 3건·659만원·정밀도 0.897). 미탐(FN) 13건 동일.

**온도 사각지대**: 온도 계통 불량 11건 중 탐지 **0건(0%)**. 미탐 13건 중 11건이 온도.

---

## 2. Chart.js 바로 쓰는 데이터 (복사해서 사용)

```js
// ⚠️ results/slide_data.json 에서 추출한 확정값 — 수정 금지
const SLIDE_DATA = {
  // ROC 곡선 (x=FPR, y=TPR) · AUC 0.9254
  roc: [
    {x:0,y:0},{x:0.0224,y:0.6923},{x:0.0627,y:0.7692},{x:0.106,y:0.7692},
    {x:0.147,y:0.8205},{x:0.1903,y:0.8205},{x:0.2321,y:0.8462},{x:0.2746,y:0.8718},
    {x:0.3164,y:0.8974},{x:0.3597,y:0.8974},{x:0.4007,y:0.9487},{x:0.4433,y:0.9744},
    {x:0.4858,y:0.9744},{x:0.5276,y:1.0},{x:0.5709,y:1.0},{x:0.6134,y:1.0},
    {x:0.6567,y:1.0},{x:0.6993,y:1.0},{x:0.7425,y:1.0},{x:0.7851,y:1.0},
    {x:0.8284,y:1.0},{x:0.8709,y:1.0},{x:0.9142,y:1.0},{x:0.9567,y:1.0},{x:1.0,y:1.0}
  ],
  // PR 곡선 (x=Recall, y=Precision) · AUC 0.7080
  pr: [
    {x:0.0256,y:1.0},{x:0.6923,y:0.4655},{x:0.7692,y:0.2609},{x:0.7692,y:0.1734},
    {x:0.8205,y:0.1391},{x:0.8205,y:0.1111},{x:0.8462,y:0.0957},{x:0.8718,y:0.0846},
    {x:0.8974,y:0.0761},{x:0.8974,y:0.0677},{x:0.9487,y:0.0643},{x:0.9744,y:0.0601},
    {x:0.9744,y:0.0551},{x:1.0,y:0.0522},{x:1.0,y:0.0485},{x:1.0,y:0.0452},
    {x:1.0,y:0.0424},{x:1.0,y:0.0399},{x:1.0,y:0.0377},{x:1.0,y:0.0357},
    {x:1.0,y:0.0339},{x:1.0,y:0.0323},{x:1.0,y:0.0309},{x:1.0,y:0.0295},{x:1.0,y:0.0283}
  ],
  // 혼동행렬
  confusion: { TN:1334, FP:6, FN:13, TP:26 },
  // 합의 알고리즘 비교 (거짓경보 FP / 정밀도 / 재현율 / F1)
  consensus: {
    labels:   ["≥1/4 합집합","≥2/4 다수결","≥3/4 권장","≥4/4 만장일치"],
    fp:        [31, 9, 5, 3],
    precision: [0.50, 0.75, 0.844, 0.897],
    recall:    [0.795, 0.692, 0.692, 0.667],
    f1:        [0.614, 0.72, 0.761, 0.765]
  },
  // 비용 임계 (FN 50만 / FP 3만)
  cost: { deployTau:0.3198, deployFP:6, deployCost:668,
          optTau:0.5171, optFP:3, optCost:659, optPrecision:0.8966 },
  // 불량 원인 Pareto (39건 주원인 센서)
  pareto: {
    labels: ["금형온도4","최대 사출속도","최대 배압","충전 시간","가소화 시간"],
    counts: [11, 10, 8, 8, 2]   // 합 39, 상위4 누적 95%
  },
  // 계통별 탐지 커버리지 (불량 / 탐지TP / 미탐FN / 탐지율%)
  coverage: {
    labels:   ["온도","시간","속도","압력"],
    defects:  [11, 10, 10, 8],
    detected: [0, 8, 10, 8],     // 하늘색
    missed:   [11, 2, 0, 0],     // 빨강 (= defects - detected)
    rate:     [0, 80, 100, 100]  // %
  },
  // 탐지된 32건의 강도 분포
  severity: { labels:["경고","위험","긴급"], counts:[3, 4, 25] },
  // 데이터 구성
  counts: { total:1379, normal:1340, defect:39 }
};
```

---

## 3. 차트별 권장 표현

| 슬라이드 | 차트 | Chart.js type | 데이터 | 주의 |
|---|---|---|---|---|
| 8 성능 | ROC 곡선 | `line`(x선형) | `SLIDE_DATA.roc` | 대각선(0,0)-(1,1) 점선 reference 추가, 면적 fill |
| 8 성능 | PR 곡선 | `line` | `SLIDE_DATA.pr` | y=Precision은 0~1, 우하향 정상 |
| 8 성능 | 혼동행렬 | (차트X) CSS 2×2 그리드 | `confusion` | TN/TP=시안, FP/FN=빨강 |
| 6 판정 | 합의 비교 | `bar` | `consensus.fp` | "거짓경보 31→5" 강조, ≥3/4 막대만 시안 |
| 9 정직성 | 커버리지 | `bar`(stacked) | `coverage.detected`+`missed` | 온도 막대 전부 빨강(미탐11) |
| 6/9 | 불량 Pareto | `bar` + 누적선(`line`) | `pareto` | 막대 전부 빨강, 누적% 보조축 |
| 7 비용 | 비용 비교 | `bar` 2개 | `cost` | 668→659, FP 6→3 |

**색상(공통):** 정상/AI=시안 `#00D4FF`, 이상/위험=빨강 `#D42121`, 경고=앰버 `#FFA756`, 배경 `#0D0D0F`, 텍스트 `#F5F5F5`, 그리드선 `rgba(255,255,255,0.08)`.

---

## 4. Chart.js 예시 스니펫 (ROC — 그대로 사용 가능)

```js
new Chart(document.getElementById('roc'), {
  type: 'line',
  data: {
    datasets: [
      { label:'ROC (AUC 0.9254)', data: SLIDE_DATA.roc,
        borderColor:'#00D4FF', backgroundColor:'rgba(0,212,255,0.12)',
        fill:true, tension:0, pointRadius:0, borderWidth:2 },
      { label:'무작위', data:[{x:0,y:0},{x:1,y:1}],
        borderColor:'rgba(255,255,255,0.25)', borderDash:[4,4], pointRadius:0, fill:false }
    ]
  },
  options: {
    responsive:true, maintainAspectRatio:false,
    scales:{ x:{type:'linear',min:0,max:1,title:{display:true,text:'FPR',color:'#9a9aa0'},
                 grid:{color:'rgba(255,255,255,0.08)'},ticks:{color:'#9a9aa0'}},
             y:{min:0,max:1,title:{display:true,text:'TPR',color:'#9a9aa0'},
                 grid:{color:'rgba(255,255,255,0.08)'},ticks:{color:'#9a9aa0'}} },
    plugins:{ legend:{labels:{color:'#F5F5F5'}} }
  }
});
```

```js
// 커버리지 (stacked bar) — 온도 미탐 0% 강조
new Chart(document.getElementById('cov'), {
  type:'bar',
  data:{ labels:SLIDE_DATA.coverage.labels,
    datasets:[
      {label:'탐지(TP)', data:SLIDE_DATA.coverage.detected, backgroundColor:'#00D4FF'},
      {label:'미탐(FN)', data:SLIDE_DATA.coverage.missed,  backgroundColor:'#D42121'}
    ]},
  options:{ responsive:true, maintainAspectRatio:false,
    scales:{ x:{stacked:true,ticks:{color:'#F5F5F5'},grid:{display:false}},
             y:{stacked:true,ticks:{color:'#9a9aa0'},grid:{color:'rgba(255,255,255,0.08)'}} },
    plugins:{ legend:{labels:{color:'#F5F5F5'}} } }
});
```

---

## 5. 검증 체크리스트 (슬라이드 완성 후 대조)
- [ ] ROC-AUC = 0.9254 (0.92, 0.93 등으로 바뀌지 않았나)
- [ ] 혼동행렬 합 = 1379 (1334+6+13+26)
- [ ] Recall 26/39, Precision 26/32 (분모 확인)
- [ ] Pareto 합 = 39 (11+10+8+8+2)
- [ ] 커버리지 온도 미탐 11 = 불량11 (탐지0)
- [ ] 합의 FP: 31 → 5 (감소 방향 맞나)
- [ ] τ = 0.3198 ≈ 0.320 (0.184 같은 옛값 아님)
- [ ] soft 점수는 어디에도 없음 (제거됨)
