# MVP 시연 영상 — 시나리오 & 대본 (핵심 위주 · ~4분 30초)

> 목적: **"실제로 동작한다"**(MVP 완성도) 증명. 꼭 보여줄 것 위주로 압축.
> [ ] 안은 조작 지시(읽지 말 것). 나레이션은 구어체 — 그대로 읽으면 됨.
> **녹화 전 필수**: 백엔드·프론트 켜기 → 7개 탭 미리 한 번씩 방문(컴파일 워밍업) → "⚠ 백엔드 미연결" 없는지 확인 → 자연어 보고서·AI 개선안 한 번씩 미리 띄워보기. **녹화 시작 후 강력 새로고침 금지(이력 초기화됨).**

## 꼭 보여줄 5가지 (이게 핵심)
1. **실시간 탐지** — 시나리오 즉시 판정 + LIVE 스트림(매초 갱신)
2. **한 샷 → 전 탭 동기화** — 이상 샷 선택이 원인분석·안전으로 흐름
3. **XAI** — SHAP 원인 + 자연어 처방
4. **What-if** — 센서 되돌리면 복원오차 하강
5. ★ **AI 자가 개선** — 사각지대 → 개선 코드 다운로드 (최대 차별점)

---

## [0:00–0:18] 인트로
[화면: 실시간 진단(Tab1) 전체]
"사출성형 불량을 실시간으로 탐지하고, 왜 그런지 설명하며, 자기 한계까지 스스로 진단하는 플랫폼, SmartFactory XAI입니다. 모든 화면은 실제 모델이 실시간 추론한 결과입니다."

## [0:18–1:30] ① 실시간 탐지 (핵심)
[화면: Tab1]
"먼저 실시간 진단입니다."
[**'정상' 클릭**, 1초]
"정상일 때 — 상태 정상, 4개 AI 모두 잠잠, 게이지 안전."
[**'긴급' 클릭**, 화면 빨개지면 2초]
"이상 데이터가 들어오면 즉시 판정이 바뀝니다. 상태 긴급, 4개 AI 전부 발동, 복원오차가 임계값의 10배, 24개 센서 중 비정상이 빨갛게 — 모델이 그 자리에서 다시 판단한 겁니다."
[**'▶ 실측 KAMP 스트림' 클릭**, 12초간 흐르게]
"여기가 진짜 실시간 탐지입니다. 실제 검증 데이터를 매초 한 샷씩 모델에 흘려보냅니다. 매 순간 판정이 갱신되고, 아래 누적 불량률과 이상 이력이 실시간으로 쌓입니다."
[**'■ 정지'**]

## [1:30–2:20] ② 한 샷 선택 → 전 탭 동기화 (차별점)
[화면: 이력에 드롭다운 등장]
"정지하면, 지금까지 잡은 이상 샷들을 시간순으로 다시 볼 수 있습니다."
[**이력에서 위험 샷 한 줄 클릭** (또는 드롭다운 선택), 1초]
"이 샷을 선택하면 — 카드가 그 샷으로 고정되고, What-if 슬라이더까지 이 샷 기준으로 바뀝니다."
[**'→ 원인분석으로' 클릭**, Tab2로 이동]
"그리고 이 샷 그대로 원인분석으로 넘어갑니다."

## [2:20–3:00] ③ 왜 그런지 — XAI
[화면: Tab2, '📡 라이브 샷 분석 중' 배너]
"같은 샷을 분석 중이라는 배너가 떠 있죠.
[SHAP 막대 가리키며] 어느 센서가 원인인지 — 사출속도가 1번 원인입니다.
[워터폴 가리키며] 평균 정상 기준에서 각 센서가 복원오차를 얼마씩 키웠는지 분해하고,
[자연어 보고서 가리키며] 마지막으로 AI가 원인·대처·리소스 순서로, 작업자가 바로 행동할 수 있게 글로 써줍니다."
*(여유 시)* [사이드바 안전 탭 잠깐] "같은 샷이 안전 탭에선 과압 위험으로도 자동 변환됩니다."

## [3:00–3:25] ④ What-if 시뮬레이션
[화면: Tab1 복귀, What-if 슬라이더]
"이 센서를 정상으로 되돌리면 어떻게 될까요?"
[**슬라이더 오른쪽으로 천천히 드래그**, 숫자 하강 보여주기]
"슬라이더를 움직이면 복원오차가 실시간으로 떨어집니다 — '이 센서만 잡으면 정상으로 돌아온다'를 즉석에서 확인합니다."

## [3:25–4:15] ⑤ AI 자가 개선 (최대 차별점)
[화면: Tab4 설비 예지정비 → 모델 사각지대 진단]
"마지막, 저희의 핵심 차별점입니다. 우리는 모델의 약점도 드러냅니다 — **온도 불량은 탐지율이 0%**, 네 모델 전부가 온도에 둔감합니다."
[**'🤖 AI 개선안 받기' 클릭**, 분석 대기 — 편집으로 대기 컷 자르거나 멘트로 채움]
"이 진단을 AI가 평가해서 — '온도 전용 분류기를 추가하라'는 개선안과 함께,"
[추천 패널 + 다운로드 버튼 보이면]
"**실제로 돌아가는 학습 노트북과 개선 계획서를 그 자리에서 만들어 다운로드**해줍니다."
[**다운로드 버튼 클릭** → 파일 받아지는 것 보여주기]
"탐지를 넘어, 자기 약점을 메울 코드까지 스스로 제안하는 AI입니다."

## [4:15–4:30] 마무리
[화면: Tab1 또는 랜딩]
"설명할 수 있고, 정직하며, 스스로 개선하는 AI. 모든 기능은 로컬에서 실제 모델로 동작합니다. 감사합니다."

---

## 분량 조절
- **4분 30초 넘기면**: ③의 안전 탭 *(여유)* 컷 / AI 개선안 대기 컷을 편집으로 단축.
- **더 줄이려면(3분 30초)**: ④ What-if를 10초로, ②에서 드롭다운 설명 생략.
- **절대 사수**: ① 실시간 탐지(LIVE 스트림), ② 전 탭 동기화, ⑤ AI 자가 개선.

## 녹화 팁
- OBS: 디스플레이 캡처 + 마이크 오디오 입력. 마이크 레벨 확인.
- 게임바: `Win+Alt+M`(마이크 켜기) → `Win+Alt+R`(녹화).
- 해상도 1920×1080, 브라우저 전체화면(F11), 마우스 천천히, 클릭 후 1~2초 멈춤.
- **AI 개선안 버튼은 5~8초 걸림** → 누른 뒤 멘트로 채우거나, 받아진 화면만 따로 찍어 편집으로 붙이기.
- 자연어 보고서는 미리 한 번 띄워두면 매끄러움(2초 생성).
- 자막으로 가도 됨(마이크 부담 시) — 위 나레이션을 짧게 끊어 자막 처리.
